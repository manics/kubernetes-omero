# Default values for omero-server
replicaCount: 1

server:
  image:
    repository: openmicroscopy/omero-server
    tag: 5.4.1
    pullPolicy: IfNotPresent
  #resources: {}
  databaseHost:
  defaultRootPassword: ChAnGeMe
  config:
    set: {}
    append: {}

# TODO: Not yet implemented
persistence:
  enabled: true
  existingClaim:
  storageClass:
  accessMode: ReadWriteOnce
  annotations: {}
  size: 10Gi
  subPath:

service:
  name: omero-server
  type: ClusterIP
  externalPort: 4064

# TODO: Not yet implemented
ingress:
  enabled: false
  hosts:
  annotations:
  tls:

postgresql:
  enabled: true
  # See https://github.com/kubernetes/charts/tree/master/stable/postgresql#configuration
  postgresUser: omero
  # TODO: Use the autogenerated postgres secret instead
  postgresPassword: ChAnGeMe
  postgresDatabase: omero
  #persistence:
  #resources
